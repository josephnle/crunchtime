{{> header}}

{{> nav}}

<div class="container">
  <table class="table">
      {{#each courses}}
        <tr>
          <td>
            <strong>{{name}}</strong>
          </td>
        </tr>
      {{/each}}
  </table>
</div>

{{> footer}}
